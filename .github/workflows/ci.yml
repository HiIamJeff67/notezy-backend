name: Notezy CI

on:
  pull_request:
  push:
    branches:
      - main
      - "refactor/monolith-to-microservices"
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: notezy-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  GOTOOLCHAIN: local

jobs:
  format-and-vet:
    name: Format and vet
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26.x"
          cache: false
      - name: Check formatting
        run: make ci-format
      - name: Run vet for every module
        run: make ci-vet

  unit:
    name: Unit tests (${{ matrix.module }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - module: contracts
            artifact: contracts
          - module: shared
            artifact: shared
          - module: internal/cli
            artifact: internal-cli
          - module: internal/core
            artifact: internal-core
          - module: internal/gateway
            artifact: internal-gateway
          - module: internal/durablejob
            artifact: internal-durablejob
          - module: internal/email
            artifact: internal-email
          - module: internal/realtimegateway
            artifact: internal-realtimegateway
          - module: internal/notification
            artifact: internal-notification
          - module: test
            artifact: test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26.x"
          cache: false
      - name: Test module
        working-directory: ${{ matrix.module }}
        run: |
          set -o pipefail
          mkdir -p test-results
          GOWORK=off go test -json -count=1 -coverprofile=coverage.out ./... | tee test-results/go-test.json
      - name: Upload coverage profile
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ matrix.artifact }}
          path: |
            ${{ matrix.module }}/coverage.out
            ${{ matrix.module }}/test-results/go-test.json
          if-no-files-found: ignore
          retention-days: 14

  race:
    name: Race detector (${{ matrix.module }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - module: contracts
          - module: shared
          - module: internal/core
          - module: internal/gateway
          - module: internal/durablejob
          - module: internal/email
          - module: internal/realtimegateway
          - module: internal/notification
          - module: test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26.x"
          cache: false
      - name: Run race detector
        working-directory: ${{ matrix.module }}
        run: GOWORK=off go test -race -count=1 ./...

  generated-contracts:
    name: Generated contract check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26.x"
          cache: false
      - name: Regenerate GraphQL contracts
        run: make ci-generated

  containers:
    name: Container build (${{ matrix.runtime }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - runtime: gateway
            dockerfile: internal/gateway/Dockerfile
            target: production
          - runtime: core
            dockerfile: internal/core/Dockerfile
            target: production
          - runtime: durablejob
            dockerfile: internal/durablejob/Dockerfile
            target: production
          - runtime: email
            dockerfile: internal/email/Dockerfile
            target: production
          - runtime: realtimegateway
            dockerfile: internal/realtimegateway/Dockerfile
            target: production
          - runtime: notification
            dockerfile: internal/notification/Dockerfile
            target: production
          - runtime: yjsworker
            dockerfile: internal/yjsworker/Dockerfile
            target: runtime
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          target: ${{ matrix.target }}
          push: false
          tags: notezy-ci-${{ matrix.runtime }}:${{ github.sha }}

  publish-images:
    name: Publish tagged images
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [format-and-vet, unit, race, generated-contracts, containers]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        include:
          - runtime: gateway
            dockerfile: internal/gateway/Dockerfile
            target: production
          - runtime: core
            dockerfile: internal/core/Dockerfile
            target: production
          - runtime: durablejob
            dockerfile: internal/durablejob/Dockerfile
            target: production
          - runtime: email
            dockerfile: internal/email/Dockerfile
            target: production
          - runtime: realtimegateway
            dockerfile: internal/realtimegateway/Dockerfile
            target: production
          - runtime: notification
            dockerfile: internal/notification/Dockerfile
            target: production
          - runtime: yjsworker
            dockerfile: internal/yjsworker/Dockerfile
            target: runtime
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Normalize GHCR repository name
        id: image-repository
        run: |
          repository="$(printf '%s' "$GITHUB_REPOSITORY" | tr '[:upper:]' '[:lower:]')"
          echo "registry=ghcr.io/${repository}" >> "$GITHUB_OUTPUT"
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          target: ${{ matrix.target }}
          push: true
          tags: ${{ steps.image-repository.outputs.registry }}/notezy-${{ matrix.runtime }}:${{ github.ref_name }}
