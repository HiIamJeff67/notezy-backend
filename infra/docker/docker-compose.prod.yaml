services:
  notezy-redis:
    container_name: ${DOCKER_REDIS_SERVICE_NAME}
    # restart: always
    image: redis:7.2.3-alpine
    volumes:
      - redis-data:/data
    command: ["redis-server", "--appendonly", "yes"]

  notezy-db:
    # should be the same as DB_HOST(hosting service)
    container_name: ${DOCKER_DB_SERVICE_NAME}
    # restart: always
    image: postgres:16
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # LGTM stack: keep telemetry storage private to the Compose network.
  notezy-loki:
    container_name: ${DOCKER_LOKI_SERVICE_NAME:-notezy-loki}
    image: grafana/loki:latest
    command: ["-config.file=/etc/loki.yaml", "-config.expand-env=true"]
    environment:
      DOCKER_LOKI_PORT: ${DOCKER_LOKI_PORT:-3100}
    expose:
      - "${DOCKER_LOKI_PORT:-3100}"
    volumes:
      - ../../infra/monitor/loki-config.dev.yaml:/etc/loki.yaml:ro
      - loki-data:/loki

  notezy-mimir-init:
    image: busybox:1.37.0
    command:
      - sh
      - -ec
      - |
        mkdir -p \
          /data/mimir/blocks \
          /data/mimir/tsdb \
          /data/mimir/sync \
          /data/mimir/compactor \
          /data/mimir/alertmanager \
          /data/mimir/alertmanager-configs
    volumes:
      - mimir-data:/data/mimir
    restart: "no"

  notezy-mimir:
    container_name: ${DOCKER_MIMIR_SERVICE_NAME:-notezy-mimir}
    image: grafana/mimir:latest
    command:
      ["-config.file=/etc/mimir.yaml", "-target=all,alertmanager", "-config.expand-env=true"]
    environment:
      DOCKER_MIMIR_PORT: ${DOCKER_MIMIR_PORT:-9009}
    expose:
      - "${DOCKER_MIMIR_PORT:-9009}"
    volumes:
      - ../../infra/monitor/mimir-config.dev.yaml:/etc/mimir.yaml:ro
      - ../../infra/monitor/alertmanager-fallback.dev.yaml:/etc/mimir/alertmanager-fallback.yaml:ro
      - ../../infra/monitor/alerts:/etc/mimir/rules/anonymous:ro
      - mimir-data:/data/mimir
    depends_on:
      notezy-mimir-init:
        condition: service_completed_successfully

  notezy-tempo:
    container_name: ${DOCKER_TEMPO_SERVICE_NAME:-notezy-tempo}
    image: grafana/tempo:2.9.1
    command: ["-config.file=/etc/tempo.yaml", "-config.expand-env=true"]
    environment:
      TEMPO_METRICS_CLUSTER: ${TEMPO_METRICS_CLUSTER:-lgtm-production}
      DOCKER_TEMPO_PORT: ${DOCKER_TEMPO_PORT:-3200}
      DOCKER_OTEL_COLLECTOR_GRPC_PORT: ${DOCKER_OTEL_COLLECTOR_GRPC_PORT:-4317}
      DOCKER_OTEL_COLLECTOR_HTTP_PORT: ${DOCKER_OTEL_COLLECTOR_HTTP_PORT:-4318}
      DOCKER_MIMIR_SERVICE_NAME: ${DOCKER_MIMIR_SERVICE_NAME:-notezy-mimir}
      DOCKER_MIMIR_PORT: ${DOCKER_MIMIR_PORT:-9009}
    expose:
      - "${DOCKER_TEMPO_PORT:-3200}"
    volumes:
      - ../../infra/monitor/tempo-config.dev.yaml:/etc/tempo.yaml:ro
      - tempo-data:/var/tempo
    depends_on:
      notezy-mimir:
        condition: service_started

  notezy-otel-collector:
    container_name: ${DOCKER_OTEL_COLLECTOR_SERVICE_NAME:-notezy-otel-collector}
    image: otel/opentelemetry-collector-contrib:latest
    environment:
      OTEL_RESOURCE_CLUSTER: ${OTEL_RESOURCE_CLUSTER:-lgtm-production}
      DOCKER_LOKI_SERVICE_NAME: ${DOCKER_LOKI_SERVICE_NAME:-notezy-loki}
      DOCKER_TEMPO_SERVICE_NAME: ${DOCKER_TEMPO_SERVICE_NAME:-notezy-tempo}
      DOCKER_MIMIR_SERVICE_NAME: ${DOCKER_MIMIR_SERVICE_NAME:-notezy-mimir}
      DOCKER_OTEL_COLLECTOR_GRPC_PORT: ${DOCKER_OTEL_COLLECTOR_GRPC_PORT:-4317}
      DOCKER_OTEL_COLLECTOR_HTTP_PORT: ${DOCKER_OTEL_COLLECTOR_HTTP_PORT:-4318}
    expose:
      - "${DOCKER_OTEL_COLLECTOR_GRPC_PORT:-4317}"
      - "${DOCKER_OTEL_COLLECTOR_HTTP_PORT:-4318}"
    volumes:
      - ../../infra/monitor/otel-collector-config.dev.yaml:/etc/otelcol-contrib/config.yaml:ro
    depends_on:
      notezy-loki:
        condition: service_started
      notezy-mimir:
        condition: service_started
      notezy-tempo:
        condition: service_started

  notezy-grafana:
    container_name: ${DOCKER_GRAFANA_SERVICE_NAME:-notezy-grafana}
    image: grafana/grafana:latest
    environment:
      GF_SECURITY_ADMIN_PASSWORD: ${GF_SECURITY_ADMIN_PASSWORD}
      GF_FEATURE_TOGGLE_ENABLE: ${GF_FEATURE_TOGGLE_ENABLE:-traceqlEditor}
      DOCKER_MIMIR_SERVICE_NAME: ${DOCKER_MIMIR_SERVICE_NAME:-notezy-mimir}
      DOCKER_MIMIR_PORT: ${DOCKER_MIMIR_PORT:-9009}
      DOCKER_TEMPO_SERVICE_NAME: ${DOCKER_TEMPO_SERVICE_NAME:-notezy-tempo}
      DOCKER_TEMPO_PORT: ${DOCKER_TEMPO_PORT:-3200}
      DOCKER_LOKI_SERVICE_NAME: ${DOCKER_LOKI_SERVICE_NAME:-notezy-loki}
      DOCKER_LOKI_PORT: ${DOCKER_LOKI_PORT:-3100}
    ports:
      - "${GRAFANA_PORT:-3000}:${DOCKER_GRAFANA_PORT:-3000}"
    volumes:
      - ../../infra/monitor/grafana/datasources.dev.yaml:/etc/grafana/provisioning/datasources/datasources.yaml:ro
      - ../../infra/monitor/grafana/dashboards.dev.yaml:/etc/grafana/provisioning/dashboards/dashboards.yaml:ro
      - ../../infra/monitor/grafana/dashboards:/var/lib/grafana/dashboards:ro
      - grafana-data:/var/lib/grafana
    depends_on:
      notezy-loki:
        condition: service_started
      notezy-tempo:
        condition: service_started
      notezy-mimir:
        condition: service_started

  notezy-client-gateway:
    container_name: ${DOCKER_CLIENT_GATEWAY_SERVICE_NAME:-notezy-client-gateway}
    image: ${CLIENT_GATEWAY_IMAGE:-notezy-client-gateway:latest}
    # restart: always
    build:
      context: ../..
      dockerfile: internal/clientgateway/Dockerfile
      target: production
    # entrypoint: ["${ENTRYPOINT_CMD}"]
    environment:
      REDIS_HOST: ${DOCKER_REDIS_SERVICE_NAME}
      REDIS_PORT: ${DOCKER_REDIS_PORT}
      REDIS_PASSWORD: ${REDIS_PASSWORD:-}
      REDIS_INIT_DB: ${REDIS_INIT_DB:-0}
      CLIENT_GATEWAY_LISTEN_ADDRESS: 0.0.0.0:${DOCKER_GIN_PORT}
      GIN_TRUSTED_PROXIES: ${GIN_TRUSTED_PROXIES}
      JWT_ACCESS_TOKEN_SECRET_KEY: ${JWT_ACCESS_TOKEN_SECRET_KEY}
      JWT_REFRESH_TOKEN_SECRET_KEY: ${JWT_REFRESH_TOKEN_SECRET_KEY}
      CORE_DELEGATION_SECRET: ${CORE_DELEGATION_SECRET}
      CORE_DELEGATION_AUDIENCE: ${CORE_DELEGATION_AUDIENCE}
      CORE_DELEGATION_ISSUER: ${CORE_DELEGATION_ISSUER}
      CORE_BASE_URL: ${CORE_BASE_URL:-http://notezy-core:7778}
      CORE_CLIENT_TIMEOUT: ${CORE_CLIENT_TIMEOUT:-10s}
      NOTIFICATION_BASE_URL: ${NOTIFICATION_BASE_URL:-http://notezy-notification:7781}
      NOTIFICATION_CLIENT_TIMEOUT: ${NOTIFICATION_CLIENT_TIMEOUT:-10s}
      OTEL_SERVICE_NAME: notezy-client-gateway
      OTEL_SERVICE_VERSION: ${OTEL_SERVICE_VERSION:-unknown}
      OTEL_DEPLOYMENT_ENVIRONMENT: production
      OTEL_EXPORTER_OTLP_GRPC_ENDPOINT: ${OTEL_EXPORTER_OTLP_GRPC_ENDPOINT}
      ALLOWED_DOMAINS: ${ALLOWED_DOMAINS}
    ports:
      - "${GIN_PORT}:${DOCKER_GIN_PORT}"
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://127.0.0.1:${DOCKER_GIN_PORT}/healthz || exit 1"]
      interval: 60s
      timeout: 5s
      retries: 6
      start_period: 10s
    depends_on:
      notezy-core:
        condition: service_healthy
      notezy-notification:
        condition: service_healthy
      notezy-otel-collector:
        condition: service_started

  notezy-api-gateway:
    container_name: ${DOCKER_API_GATEWAY_SERVICE_NAME:-notezy-api-gateway}
    image: ${API_GATEWAY_IMAGE:-notezy-api-gateway:latest}
    build:
      context: ../..
      dockerfile: internal/apigateway/Dockerfile
      target: production
    environment:
      REDIS_HOST: ${DOCKER_REDIS_SERVICE_NAME}
      REDIS_PORT: ${DOCKER_REDIS_PORT}
      REDIS_PASSWORD: ${REDIS_PASSWORD:-}
      REDIS_INIT_DB: ${REDIS_INIT_DB:-0}
      API_GATEWAY_LISTEN_ADDRESS: 0.0.0.0:${DOCKER_API_GATEWAY_PORT:-7780}
      GIN_TRUSTED_PROXIES: ${GIN_TRUSTED_PROXIES}
      CORE_DELEGATION_SECRET: ${CORE_DELEGATION_SECRET}
      CORE_DELEGATION_AUDIENCE: ${CORE_DELEGATION_AUDIENCE}
      CORE_DELEGATION_ISSUER: ${CORE_DELEGATION_ISSUER}
      CORE_BASE_URL: ${CORE_BASE_URL:-http://notezy-core:7778}
      CORE_CLIENT_TIMEOUT: ${CORE_CLIENT_TIMEOUT:-10s}
      OTEL_SERVICE_NAME: notezy-api-gateway
      OTEL_SERVICE_VERSION: ${OTEL_SERVICE_VERSION:-unknown}
      OTEL_DEPLOYMENT_ENVIRONMENT: production
      OTEL_EXPORTER_OTLP_GRPC_ENDPOINT: ${OTEL_EXPORTER_OTLP_GRPC_ENDPOINT}
      ALLOWED_DOMAINS: ${ALLOWED_DOMAINS}
    ports:
      - "${API_GATEWAY_PORT:-7780}:${DOCKER_API_GATEWAY_PORT:-7780}"
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://127.0.0.1:${DOCKER_API_GATEWAY_PORT:-7780}/healthz || exit 1"]
      interval: 60s
      timeout: 5s
      retries: 6
      start_period: 10s
    depends_on:
      notezy-core:
        condition: service_healthy
      notezy-redis:
        condition: service_healthy
      notezy-otel-collector:
        condition: service_started

  notezy-core:
    container_name: ${DOCKER_CORE_SERVICE_NAME:-notezy-core}
    image: ${CORE_IMAGE:-notezy-core:latest}
    build:
      context: ../..
      dockerfile: internal/core/Dockerfile
      target: production
    volumes:
      - ../../secrets/kafka:/run/secrets/kafka:ro
    environment:
      DB_HOST: ${DB_HOST}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
      DOCKER_DB_PORT: ${DOCKER_DB_PORT}
      REDIS_HOST: ${DOCKER_REDIS_SERVICE_NAME}
      REDIS_PORT: ${DOCKER_REDIS_PORT}
      REDIS_PASSWORD: ${REDIS_PASSWORD:-}
      REDIS_INIT_DB: ${REDIS_INIT_DB:-0}
      CORE_USER_DATA_CACHE_EXPIRES_IN: ${CORE_USER_DATA_CACHE_EXPIRES_IN:-1h}
      CORE_QUOTA_CYCLE_WORKER_INTERVAL: ${CORE_QUOTA_CYCLE_WORKER_INTERVAL:-24h}
      KAFKA_BROKERS: ${KAFKA_BROKERS:-notezy-kafka:9092}
      KAFKA_DIAL_TIMEOUT: ${KAFKA_DIAL_TIMEOUT:-3s}
      KAFKA_TLS_ENABLED: ${KAFKA_TLS_ENABLED:-false}
      KAFKA_TLS_CA_FILE: ${KAFKA_TLS_CA_FILE:-/run/secrets/kafka/ca.pem}
      KAFKA_TLS_CERT_FILE: ${KAFKA_TLS_CERT_FILE:-}
      KAFKA_TLS_KEY_FILE: ${KAFKA_TLS_KEY_FILE:-}
      KAFKA_TLS_SERVER_NAME: ${KAFKA_TLS_SERVER_NAME:-}
      KAFKA_SASL_MECHANISM: ${KAFKA_SASL_MECHANISM:-}
      KAFKA_SASL_USERNAME: ${KAFKA_SASL_USERNAME:-}
      KAFKA_SASL_PASSWORD: ${KAFKA_SASL_PASSWORD:-}
      CORE_LISTEN_ADDRESS: 0.0.0.0:7778
      JWT_ACCESS_TOKEN_SECRET_KEY: ${JWT_ACCESS_TOKEN_SECRET_KEY}
      JWT_REFRESH_TOKEN_SECRET_KEY: ${JWT_REFRESH_TOKEN_SECRET_KEY}
      CORE_DELEGATION_SECRET: ${CORE_DELEGATION_SECRET}
      CORE_DELEGATION_AUDIENCE: ${CORE_DELEGATION_AUDIENCE}
      CORE_DELEGATION_ISSUER: ${CORE_DELEGATION_ISSUER}
      REALTIME_TICKET_PRIVATE_KEY_BASE64: ${REALTIME_TICKET_PRIVATE_KEY_BASE64}
      STORAGE_KEY_SALT: ${STORAGE_KEY_SALT}
      YJS_DOCUMENT_INITIALIZATION_WORKER_URL: ${YJS_DOCUMENT_INITIALIZATION_WORKER_URL:-http://notezy-yjs-worker:8787/core/yjs-document-initialization/v1}
      YJS_DOCUMENT_INITIALIZATION_WORKER_TIMEOUT: ${YJS_DOCUMENT_INITIALIZATION_WORKER_TIMEOUT:-30s}
      KAFKA_CONSUMER_MAXIMUM_ATTEMPTS: ${KAFKA_CONSUMER_MAXIMUM_ATTEMPTS:-3}
      KAFKA_CONSUMER_INITIAL_RETRY_BACKOFF: ${KAFKA_CONSUMER_INITIAL_RETRY_BACKOFF:-250ms}
      KAFKA_CONSUMER_MAXIMUM_RETRY_BACKOFF: ${KAFKA_CONSUMER_MAXIMUM_RETRY_BACKOFF:-5s}
      KAFKA_CONSUMER_MAXIMUM_POLL_RECORDS: ${KAFKA_CONSUMER_MAXIMUM_POLL_RECORDS:-100}
      OUTBOX_RELAY_BATCH_SIZE: ${OUTBOX_RELAY_BATCH_SIZE:-100}
      OUTBOX_RELAY_POLL_INTERVAL: ${OUTBOX_RELAY_POLL_INTERVAL:-1s}
      OUTBOX_RELAY_CLAIM_TIMEOUT: ${OUTBOX_RELAY_CLAIM_TIMEOUT:-30s}
      OUTBOX_RELAY_INITIAL_BACKOFF: ${OUTBOX_RELAY_INITIAL_BACKOFF:-1s}
      OUTBOX_RELAY_MAXIMUM_BACKOFF: ${OUTBOX_RELAY_MAXIMUM_BACKOFF:-1m}
      OUTBOX_RELAY_RETENTION: ${OUTBOX_RELAY_RETENTION:-168h}
      OUTBOX_RELAY_CLEANUP_INTERVAL: ${OUTBOX_RELAY_CLEANUP_INTERVAL:-1h}
      OAUTH_GOOGLE_CLIENT_ID: ${OAUTH_GOOGLE_CLIENT_ID}
      OAUTH_GOOGLE_CLIENT_SECRET: ${OAUTH_GOOGLE_CLIENT_SECRET}
      OAUTH_GOOGLE_REDIRECT_URL: ${OAUTH_GOOGLE_REDIRECT_URL}
      OTEL_SERVICE_NAME: notezy-core
      OTEL_SERVICE_VERSION: ${OTEL_SERVICE_VERSION:-unknown}
      OTEL_DEPLOYMENT_ENVIRONMENT: production
      OTEL_EXPORTER_OTLP_GRPC_ENDPOINT: ${OTEL_EXPORTER_OTLP_GRPC_ENDPOINT}
    expose:
      - "7778"
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://127.0.0.1:7778/healthz || exit 1"]
      interval: 60s
      timeout: 5s
      retries: 6
      start_period: 10s
    depends_on:
      notezy-db:
        condition: service_healthy
      notezy-yjs-worker:
        condition: service_healthy
      notezy-otel-collector:
        condition: service_started

  notezy-realtime-gateway:
    container_name: ${DOCKER_REALTIME_GATEWAY_SERVICE_NAME:-notezy-realtime-gateway}
    image: ${REALTIME_GATEWAY_IMAGE:-notezy-realtime-gateway:latest}
    build:
      context: ../..
      dockerfile: internal/realtimegateway/Dockerfile
      target: production
    volumes:
      - ../../secrets/kafka:/run/secrets/kafka:ro
    environment:
      REDIS_HOST: ${DOCKER_REDIS_SERVICE_NAME}
      REDIS_PORT: ${DOCKER_REDIS_PORT}
      REDIS_PASSWORD: ${REDIS_PASSWORD:-}
      REDIS_INIT_DB: ${REDIS_INIT_DB:-0}
      KAFKA_BROKERS: ${KAFKA_BROKERS:-notezy-kafka:9092}
      KAFKA_DIAL_TIMEOUT: ${KAFKA_DIAL_TIMEOUT:-3s}
      KAFKA_TLS_ENABLED: ${KAFKA_TLS_ENABLED:-false}
      KAFKA_TLS_CA_FILE: ${KAFKA_TLS_CA_FILE:-/run/secrets/kafka/ca.pem}
      KAFKA_TLS_CERT_FILE: ${KAFKA_TLS_CERT_FILE:-}
      KAFKA_TLS_KEY_FILE: ${KAFKA_TLS_KEY_FILE:-}
      KAFKA_TLS_SERVER_NAME: ${KAFKA_TLS_SERVER_NAME:-}
      KAFKA_SASL_MECHANISM: ${KAFKA_SASL_MECHANISM:-}
      KAFKA_SASL_USERNAME: ${KAFKA_SASL_USERNAME:-}
      KAFKA_SASL_PASSWORD: ${KAFKA_SASL_PASSWORD:-}
      KAFKA_CONSUMER_MAXIMUM_ATTEMPTS: ${KAFKA_CONSUMER_MAXIMUM_ATTEMPTS:-3}
      KAFKA_CONSUMER_INITIAL_RETRY_BACKOFF: ${KAFKA_CONSUMER_INITIAL_RETRY_BACKOFF:-250ms}
      KAFKA_CONSUMER_MAXIMUM_RETRY_BACKOFF: ${KAFKA_CONSUMER_MAXIMUM_RETRY_BACKOFF:-5s}
      KAFKA_CONSUMER_MAXIMUM_POLL_RECORDS: ${KAFKA_CONSUMER_MAXIMUM_POLL_RECORDS:-100}
      CORE_BASE_URL: ${CORE_BASE_URL:-http://notezy-core:7778}
      CORE_DELEGATION_SECRET: ${CORE_DELEGATION_SECRET}
      CORE_DELEGATION_AUDIENCE: ${CORE_DELEGATION_AUDIENCE}
      CORE_DELEGATION_ISSUER: ${CORE_DELEGATION_ISSUER}
      REALTIME_TICKET_PUBLIC_KEY_BASE64: ${REALTIME_TICKET_PUBLIC_KEY_BASE64}
      REALTIME_ENABLED: ${REALTIME_ENABLED:-true}
      REALTIME_BETA_USER_PUBLIC_IDS: ${REALTIME_BETA_USER_PUBLIC_IDS:-}
      YJS_WORKER_URLS: ws://notezy-yjs-worker:${DOCKER_YJS_WORKER_PORT:-8787}/core/realtime/v1
      REALTIME_GATEWAY_LISTEN_ADDRESS: ${REALTIME_GATEWAY_LISTEN_ADDRESS:-0.0.0.0:7779}
      GIN_TRUSTED_PROXIES: ${GIN_TRUSTED_PROXIES}
      ALLOWED_DOMAINS: ${ALLOWED_DOMAINS}
      OTEL_SERVICE_NAME: notezy-realtime-gateway
      OTEL_SERVICE_VERSION: ${OTEL_SERVICE_VERSION:-unknown}
      OTEL_DEPLOYMENT_ENVIRONMENT: production
      OTEL_EXPORTER_OTLP_GRPC_ENDPOINT: ${OTEL_EXPORTER_OTLP_GRPC_ENDPOINT}
    expose:
      - "7779"
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://127.0.0.1:7779/healthz || exit 1"]
      interval: 60s
      timeout: 5s
      retries: 6
      start_period: 10s
    depends_on:
      notezy-core:
        condition: service_healthy
      notezy-yjs-worker:
        condition: service_healthy
      notezy-otel-collector:
        condition: service_started

  notezy-durable-job:
    container_name: ${DOCKER_DURABLE_JOB_SERVICE_NAME:-notezy-durable-job}
    image: ${DURABLE_JOB_IMAGE:-notezy-durable-job:latest}
    build:
      context: ../..
      dockerfile: internal/durablejob/Dockerfile
      target: production
    volumes:
      - ../../secrets/kafka:/run/secrets/kafka:ro
    environment:
      DB_HOST: ${DB_HOST}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
      DOCKER_DB_PORT: ${DOCKER_DB_PORT}
      KAFKA_BROKERS: ${KAFKA_BROKERS:-notezy-kafka:9092}
      KAFKA_DIAL_TIMEOUT: ${KAFKA_DIAL_TIMEOUT:-3s}
      KAFKA_TLS_ENABLED: ${KAFKA_TLS_ENABLED:-false}
      KAFKA_TLS_CA_FILE: ${KAFKA_TLS_CA_FILE:-/run/secrets/kafka/ca.pem}
      KAFKA_TLS_CERT_FILE: ${KAFKA_TLS_CERT_FILE:-}
      KAFKA_TLS_KEY_FILE: ${KAFKA_TLS_KEY_FILE:-}
      KAFKA_TLS_SERVER_NAME: ${KAFKA_TLS_SERVER_NAME:-}
      KAFKA_SASL_MECHANISM: ${KAFKA_SASL_MECHANISM:-}
      KAFKA_SASL_USERNAME: ${KAFKA_SASL_USERNAME:-}
      KAFKA_SASL_PASSWORD: ${KAFKA_SASL_PASSWORD:-}
      DURABLEJOB_LISTEN_ADDRESS: 0.0.0.0:8082
      KAFKA_CONSUMER_MAXIMUM_ATTEMPTS: ${KAFKA_CONSUMER_MAXIMUM_ATTEMPTS:-3}
      KAFKA_CONSUMER_INITIAL_RETRY_BACKOFF: ${KAFKA_CONSUMER_INITIAL_RETRY_BACKOFF:-250ms}
      KAFKA_CONSUMER_MAXIMUM_RETRY_BACKOFF: ${KAFKA_CONSUMER_MAXIMUM_RETRY_BACKOFF:-5s}
      KAFKA_CONSUMER_MAXIMUM_POLL_RECORDS: ${KAFKA_CONSUMER_MAXIMUM_POLL_RECORDS:-100}
      DURABLEJOB_YJS_MAINTENANCE_MAXIMUM_PENDING_HINTS: ${DURABLEJOB_YJS_MAINTENANCE_MAXIMUM_PENDING_HINTS:-1000}
      DURABLEJOB_YJS_MAINTENANCE_MAXIMUM_DISPATCH_BATCH: ${DURABLEJOB_YJS_MAINTENANCE_MAXIMUM_DISPATCH_BATCH:-32}
      DURABLEJOB_YJS_MAINTENANCE_MAXIMUM_DISPATCH_WORKERS: ${DURABLEJOB_YJS_MAINTENANCE_MAXIMUM_DISPATCH_WORKERS:-8}
      DURABLEJOB_YJS_MAINTENANCE_MAXIMUM_REQUEST_ATTEMPTS: ${DURABLEJOB_YJS_MAINTENANCE_MAXIMUM_REQUEST_ATTEMPTS:-3}
      OTEL_SERVICE_NAME: notezy-durable-job
      OTEL_SERVICE_VERSION: ${OTEL_SERVICE_VERSION:-unknown}
      OTEL_DEPLOYMENT_ENVIRONMENT: production
      OTEL_EXPORTER_OTLP_GRPC_ENDPOINT: ${OTEL_EXPORTER_OTLP_GRPC_ENDPOINT}
    expose:
      - "8082"
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://127.0.0.1:8082/healthz || exit 1"]
      interval: 60s
      timeout: 5s
      retries: 6
      start_period: 10s
    depends_on:
      notezy-db:
        condition: service_healthy
      notezy-core:
        condition: service_healthy
      notezy-yjs-worker:
        condition: service_healthy
      notezy-otel-collector:
        condition: service_started

  notezy-email:
    container_name: ${DOCKER_EMAIL_SERVICE_NAME:-notezy-email}
    image: ${EMAIL_IMAGE:-notezy-email:latest}
    build:
      context: ../..
      dockerfile: internal/email/Dockerfile
      target: production
    volumes:
      - ../../secrets/kafka:/run/secrets/kafka:ro
    environment:
      EMAIL_LISTEN_ADDRESS: 0.0.0.0:8081
      SMTP_HOST: ${SMTP_HOST}
      SMTP_PORT: ${SMTP_PORT}
      NOTEZY_OFFICIAL_NAME: ${NOTEZY_OFFICIAL_NAME}
      NOTEZY_OFFICIAL_GMAIL: ${NOTEZY_OFFICIAL_GMAIL}
      NOTEZY_OFFICIAL_GOOGLE_APPLICATION_PASSWORD: ${NOTEZY_OFFICIAL_GOOGLE_APPLICATION_PASSWORD}
      KAFKA_BROKERS: ${KAFKA_BROKERS}
      KAFKA_DIAL_TIMEOUT: ${KAFKA_DIAL_TIMEOUT:-3s}
      KAFKA_TLS_ENABLED: ${KAFKA_TLS_ENABLED:-false}
      KAFKA_TLS_CA_FILE: ${KAFKA_TLS_CA_FILE:-/run/secrets/kafka/ca.pem}
      KAFKA_TLS_CERT_FILE: ${KAFKA_TLS_CERT_FILE:-}
      KAFKA_TLS_KEY_FILE: ${KAFKA_TLS_KEY_FILE:-}
      KAFKA_TLS_SERVER_NAME: ${KAFKA_TLS_SERVER_NAME:-}
      KAFKA_SASL_MECHANISM: ${KAFKA_SASL_MECHANISM:-}
      KAFKA_SASL_USERNAME: ${KAFKA_SASL_USERNAME:-}
      KAFKA_SASL_PASSWORD: ${KAFKA_SASL_PASSWORD:-}
      KAFKA_CONSUMER_MAXIMUM_ATTEMPTS: ${KAFKA_CONSUMER_MAXIMUM_ATTEMPTS:-3}
      KAFKA_CONSUMER_INITIAL_RETRY_BACKOFF: ${KAFKA_CONSUMER_INITIAL_RETRY_BACKOFF:-250ms}
      KAFKA_CONSUMER_MAXIMUM_RETRY_BACKOFF: ${KAFKA_CONSUMER_MAXIMUM_RETRY_BACKOFF:-5s}
      KAFKA_CONSUMER_MAXIMUM_POLL_RECORDS: ${KAFKA_CONSUMER_MAXIMUM_POLL_RECORDS:-100}
      OTEL_SERVICE_NAME: notezy-email
      OTEL_SERVICE_VERSION: ${OTEL_SERVICE_VERSION:-unknown}
      OTEL_DEPLOYMENT_ENVIRONMENT: production
      OTEL_EXPORTER_OTLP_GRPC_ENDPOINT: ${OTEL_EXPORTER_OTLP_GRPC_ENDPOINT}
    expose:
      - "8081"
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://127.0.0.1:8081/healthz || exit 1"]
      interval: 60s
      timeout: 5s
      retries: 6
      start_period: 10s
    depends_on:
      notezy-otel-collector:
        condition: service_started
  notezy-yjs-worker:
    image: ${YJS_WORKER_IMAGE:-notezy-yjs-worker:latest}
    build:
      context: ../..
      dockerfile: internal/yjsworker/Dockerfile
    environment:
      NODE_ENV: production
      YJS_WORKER_HOST: 0.0.0.0
      YJS_WORKER_PORT: ${DOCKER_YJS_WORKER_PORT:-8787}
      KAFKA_BROKERS: ${KAFKA_BROKERS}
      KAFKA_CLIENT_ID: notezy-yjs-worker
      YJS_WORKER_COMMAND_TIMEOUT_MILLISECONDS: ${YJS_WORKER_COMMAND_TIMEOUT_MILLISECONDS:-10000}
      OTEL_SERVICE_NAME: notezy-yjs-worker
      OTEL_SERVICE_VERSION: ${OTEL_SERVICE_VERSION:-unknown}
      OTEL_DEPLOYMENT_ENVIRONMENT: production
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT}
    expose:
      - "${DOCKER_YJS_WORKER_PORT:-8787}"
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:' + process.env.YJS_WORKER_PORT + '/healthz').then(response => process.exit(response.ok ? 0 : 1)).catch(() => process.exit(1))"]
      interval: 60s
      timeout: 5s
      retries: 6
    depends_on:
      notezy-otel-collector:
        condition: service_started

  notezy-nginx:
    image: nginx:1.27-alpine
    container_name: ${DOCKER_NGINX_SERVICE_NAME}
    depends_on:
      notezy-client-gateway:
        condition: service_healthy
      notezy-api-gateway:
        condition: service_healthy
      notezy-realtime-gateway:
        condition: service_healthy
    ports:
      - "${NGINX_HTTP_PORT}:${DOCKER_NGINX_HTTP_PORT}"
      - "${NGINX_HTTPS_PORT}:${DOCKER_NGINX_HTTPS_PORT}"
    volumes:
      - ../../infra/nginx/default.prod.conf:/etc/nginx/conf.d/default.conf:ro
      - ../../infra/nginx/ssl:/etc/nginx/ssl:ro

volumes:
  redis-data:
    name: notezy-backend-redis-data
  postgres-data:
    name: notezy-backend-postgres-data
  loki-data:
    name: notezy-backend-loki-data
  tempo-data:
    name: notezy-backend-tempo-data
  mimir-data:
    name: notezy-backend-mimir-data
  grafana-data:
    name: notezy-backend-grafana-data
