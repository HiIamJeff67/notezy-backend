### Setup:

Set the mode type to the corresponded environments:

```Go
// shared/constants/project_management_constant.go
import types "notezy-backend/shared/types"

const (
    Mode types.ModeType = types.ModeType_Production // for production environment
)
```
