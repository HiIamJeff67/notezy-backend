# Development log — 2025-12-24

This snapshot is generated from commits dated 2025-12-24. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `a402363c` 2025-12-24 — feat: complete block group controllers, binders and routes & use new gorm tags to make null distinct in some unique index & continue fix issues in block group service

## Changed areas

- `app` — 18 changed-file references
- `shared` — 3 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
