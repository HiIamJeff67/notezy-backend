# Development log — 2025-12-12

This snapshot is generated from commits dated 2025-12-12. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `04ffbd02` 2025-12-12 — refactor: refactor blocknode lib & build MaxContextSizeMiddleware

## Changed areas

- `app` — 11 changed-file references
- `shared` — 10 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
