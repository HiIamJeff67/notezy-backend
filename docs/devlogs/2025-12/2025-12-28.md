# Development log — 2025-12-28

This snapshot is generated from commits dated 2025-12-28. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `a3fae9f4` 2025-12-28 — refactor: move some shared libraries to the app library

## Changed areas

- `app` — 18 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
