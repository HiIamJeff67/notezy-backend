# Development log — 2025-09-02

This snapshot is generated from commits dated 2025-09-02. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `3c94dbe2` 2025-09-02 — feat: add access permission control on shelf and material & continue on building material repository and service & refactor gorm db model to bind gorm struct tag

## Changed areas

- `app` — 25 changed-file references
- `shared` — 2 changed-file references
- `docker-compose.yaml` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
