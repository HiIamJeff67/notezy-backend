# Development log — 2025-09-17

This snapshot is generated from commits dated 2025-09-17. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `a72b3396` 2025-09-17 — debug: fix some error for shelf and material structure in root shelf and sub shelf

## Changed areas

- `app` — 19 changed-file references
- `docs` — 2 changed-file references
- `shared` — 1 changed-file references
- `go.sum` — 1 changed-file references
- `go.mod` — 1 changed-file references
- `Makefile` — 1 changed-file references
- `Dockerfile.dev` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
