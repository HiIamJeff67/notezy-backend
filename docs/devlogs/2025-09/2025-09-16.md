# Development log — 2025-09-16

This snapshot is generated from commits dated 2025-09-16. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `2507694e` 2025-09-16 — fix: fix some constraints in sub shelf
- `94a8f4c2` 2025-09-16 — feat: build the restore and soft delete and hard delete in sub shelf and root shelf & add triggers to ensure soft delete and restore operations
- `26ffc484` 2025-09-16 — refactor: using sub shelf to replace the shelf node structure

## Changed areas

- `app` — 74 changed-file references
- `shared` — 7 changed-file references
- `docs` — 2 changed-file references
- `.vscode` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
