# Development log — 2025-09-23

This snapshot is generated from commits dated 2025-09-23. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `eeacbfa6` 2025-09-23 — chore: rename some routes & indicate deleted data in mutation

## Changed areas

- `app` — 15 changed-file references
- `shared` — 3 changed-file references
- `go.mod` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
