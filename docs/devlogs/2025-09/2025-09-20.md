# Development log — 2025-09-20

This snapshot is generated from commits dated 2025-09-20. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `2358c278` 2025-09-20 — chore: fix old graphql schemas & make sure api syntax

## Changed areas

- `app` — 25 changed-file references
- `shared` — 3 changed-file references
- `go.mod` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
