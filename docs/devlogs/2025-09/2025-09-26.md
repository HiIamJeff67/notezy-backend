# Development log — 2025-09-26

This snapshot is generated from commits dated 2025-09-26. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `2f33819d` 2025-09-26 — chore: add onlyDeleted parameter in permission checking & add ternary type & modify some dtos for frontend

## Changed areas

- `app` — 11 changed-file references
- `shared` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
