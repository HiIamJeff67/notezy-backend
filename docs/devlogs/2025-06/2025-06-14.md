# Development log — 2025-06-14

This snapshot is generated from commits dated 2025-06-14. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `b689bfb8` 2025-06-14 — feat: use setNull column as dto and input to solve the partial update issue

## Changed areas

- `app` — 8 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
