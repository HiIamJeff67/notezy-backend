# Development log — 2025-06-26

This snapshot is generated from commits dated 2025-06-26. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `cede5b0c` 2025-06-26 — feat: build test packages & build unit test for util packages & move shared folder outside the app folder

## Changed areas

- `app` — 32 changed-file references
- `test` — 19 changed-file references
- `shared` — 18 changed-file references
- `go.mod` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
