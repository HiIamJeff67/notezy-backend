# Development log — 2026-05-20

This snapshot is generated from commits dated 2026-05-20. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `487c8ca7` 2026-05-20 — feat: build nginx service in docker for the proxy layer to make sure x-real-ip and sanitize the x-forwarded-for header & change to use unauthorized rate limit middleware in all the routes

## Changed areas

- `app` — 20 changed-file references
- `infra` — 3 changed-file references
- `shared` — 2 changed-file references
- `docker-compose.yaml` — 1 changed-file references
- `docker-compose.prod.yaml` — 1 changed-file references
- `Makefile` — 1 changed-file references
- `.vscode` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
