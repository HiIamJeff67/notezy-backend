# Development log — 2026-05-22

This snapshot is generated from commits dated 2026-05-22. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `3fa394a2` 2026-05-22 — refactor: refactor material to only store the file and disable abilities for notebook and textbook and learning card and workflow

## Changed areas

- `app` — 26 changed-file references
- `shared` — 5 changed-file references
- `infra` — 1 changed-file references
- `.gitignore` — 1 changed-file references
- `.DS_Store` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
