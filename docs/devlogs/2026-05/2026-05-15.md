# Development log — 2026-05-15

This snapshot is generated from commits dated 2026-05-15. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `4f17ef65` 2026-05-15 — feat: return total size of blocks while scanning them in editable block adaptor

## Changed areas

- `app` — 28 changed-file references
- `shared` — 3 changed-file references
- `go.sum` — 1 changed-file references
- `go.mod` — 1 changed-file references
- `.DS_Store` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
