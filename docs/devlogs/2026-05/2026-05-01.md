# Development log — 2026-05-01

This snapshot is generated from commits dated 2026-05-01. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `d7a85dc0` 2026-05-01 — refactor: refactor lib to other place & make sure blocknote lib do not import other packages & rename some libs

## Changed areas

- `app` — 36 changed-file references
- `shared` — 31 changed-file references
- `test` — 1 changed-file references
- `docker-compose.yaml` — 1 changed-file references
- `.vscode` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
