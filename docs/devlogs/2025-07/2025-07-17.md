# Development log — 2025-07-17

This snapshot is generated from commits dated 2025-07-17. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `6902c479` 2025-07-17 — feat: build get many SQL operation with static order for dataloader batch function & build graphql exception domain

## Changed areas

- `app` — 11 changed-file references
- `shared` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
