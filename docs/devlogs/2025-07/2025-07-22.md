# Development log — 2025-07-22

This snapshot is generated from commits dated 2025-07-22. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `3458d1f0` 2025-07-22 — fix: using compare and swap on isMonitoring of email worker manager
- `aa78014a` 2025-07-22 — feat: complete security alert email template and its sender & fix some UI issues on other email templates & eliminate the possiblity of busy waiting on tryStartMonitoring() of the email worker manager by using compare and swap

## Changed areas

- `app` — 10 changed-file references
- `docker-compose.yaml` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
