# Development log — 2025-07-03

This snapshot is generated from commits dated 2025-07-03. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `de068d59` 2025-07-03 — feat: connect and finish the register route e2e test & modify and use the interface in test structure & figure out the problem of migration while using air hotreload

## Changed areas

- `app` — 19 changed-file references
- `test` — 7 changed-file references
- `shared` — 1 changed-file references
- `docker-compose.yaml` — 1 changed-file references
- `Dockerfile.dev` — 1 changed-file references
- `.gitignore` — 1 changed-file references
- `.dockerignore` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
