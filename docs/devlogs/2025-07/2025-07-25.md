# Development log — 2025-07-25

This snapshot is generated from commits dated 2025-07-25. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `b6bc4a50` 2025-07-25 — fix: fix some issue in the cookie

## Changed areas

- `app` — 7 changed-file references
- `docker-compose.yaml` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
