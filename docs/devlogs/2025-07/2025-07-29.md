# Development log — 2025-07-29

This snapshot is generated from commits dated 2025-07-29. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `d222aca5` 2025-07-29 — refactor: refactor exception with is internal field

## Changed areas

- `app` — 14 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
