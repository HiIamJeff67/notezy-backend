# Development log — 2026-03-08

This snapshot is generated from commits dated 2026-03-08. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `52fb905f` 2026-03-08 — refactor: refactor constants/ & fix some bugs of oauth google

## Changed areas

- `app` — 43 changed-file references
- `shared` — 21 changed-file references
- `test` — 2 changed-file references
- `go.sum` — 1 changed-file references
- `go.mod` — 1 changed-file references
- `docker-compose.yaml` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
