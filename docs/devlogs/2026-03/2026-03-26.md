# Development log — 2026-03-26

This snapshot is generated from commits dated 2026-03-26. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `f466fdfc` 2026-03-26 — refactor: refactor file structure & write file structure docs & pareparing lgtm stack monitoring

## Changed areas

- `app` — 45 changed-file references
- `infra` — 7 changed-file references
- `shared` — 3 changed-file references
- `test` — 1 changed-file references
- `docs` — 1 changed-file references
- `docker-compose.yaml` — 1 changed-file references
- `docker-compose.prod.yaml` — 1 changed-file references
- `Makefile` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
