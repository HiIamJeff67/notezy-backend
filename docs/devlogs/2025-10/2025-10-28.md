# Development log — 2025-10-28

This snapshot is generated from commits dated 2025-10-28. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `a7e39b06` 2025-10-28 — feat: build get material and its parent route

## Changed areas

- `app` — 10 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
