# Development log — 2026-06-14

This snapshot is generated from commits dated 2026-06-14. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `7fdc5237` 2026-06-14 — feat: add search items graphql hook & fix missing after field in search routine graphql hook

## Changed areas

- `app` — 20 changed-file references
- `shared` — 10 changed-file references
- `docs` — 2 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
