# Development log — 2026-06-05

This snapshot is generated from commits dated 2026-06-05. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `0a484f8d` 2026-06-05 — feat: build station and routine and routine tag and routine task services & controllers & routes & related dtos and inputs & build graphql search logic on them

## Changed areas

- `app` — 17 changed-file references
- `shared` — 2 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
