# Development log — 2026-06-12

This snapshot is generated from commits dated 2026-06-12. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `e563c258` 2026-06-12 — feat: add get all apis for station and routine and routine related schemas & update database schema class diagram

## Changed areas

- `app` — 25 changed-file references
- `docs` — 2 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
