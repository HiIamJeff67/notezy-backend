# Development log — 2026-04-26

This snapshot is generated from commits dated 2026-04-26. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `3c9e9c3b` 2026-04-26 — fix: fix soft delete issues & fix tab move operation & continue on fixing delete tabbed move blocks and building insert block groups with blocks by block pack ids

## Changed areas

- `app` — 60 changed-file references
- `shared` — 3 changed-file references
- `Makefile` — 1 changed-file references
- `.vscode` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
