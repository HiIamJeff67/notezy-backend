# Development log — 2026-04-17

This snapshot is generated from commits dated 2026-04-17. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `d5376d0f` 2026-04-17 — feat: build move and batch move block group services and controllers and routes

## Changed areas

- `app` — 9 changed-file references
- `shared` — 1 changed-file references
- `Makefile` — 1 changed-file references
- `.vscode` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
