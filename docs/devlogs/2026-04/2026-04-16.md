# Development log — 2026-04-16

This snapshot is generated from commits dated 2026-04-16. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `9835c8c2` 2026-04-16 — feat: build create many and update many and bulk move on sub shelf and block pack services

## Changed areas

- `app` — 18 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
