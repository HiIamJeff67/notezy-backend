# Development log — 2026-04-27

This snapshot is generated from commits dated 2026-04-27. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `de297187` 2026-04-27 — feat: build insert block groups by block pack id and batch insert block groups by block pack ids and batch insert block groups and their blocks by block pack ids apis

## Changed areas

- `app` — 8 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
