# Development log — 2026-04-09

This snapshot is generated from commits dated 2026-04-09. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `b471e886` 2026-04-09 — fix: fix separator incorrect spelling & fix unique index on deleted at is null & fix gorm returning field in create operation

## Changed areas

- `app` — 34 changed-file references
- `.vscode` — 2 changed-file references
- `shared` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
