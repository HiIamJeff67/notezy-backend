# Development log — 2025-11-25

This snapshot is generated from commits dated 2025-11-25. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `36ebaa32` 2025-11-25 — feat: complete block pack service, controller, binder and routes

## Changed areas

- `app` — 47 changed-file references
- `shared` — 1 changed-file references
- `Makefile` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
