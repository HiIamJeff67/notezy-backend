# Development log — 2025-11-14

This snapshot is generated from commits dated 2025-11-14. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `5bdf9e75` 2025-11-14 — chore: refactor cache library name & reduce to use info level logging

## Changed areas

- `app` — 8 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
