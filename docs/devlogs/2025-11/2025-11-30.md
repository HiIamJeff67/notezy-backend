# Development log — 2025-11-30

This snapshot is generated from commits dated 2025-11-30. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `d8fc0719` 2025-11-30 — feat: complete triggers for the limitations in user account, root shelf, and block pack table

## Changed areas

- `app` — 34 changed-file references
- `shared` — 5 changed-file references
- `docs` — 1 changed-file references
- `Makefile` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
