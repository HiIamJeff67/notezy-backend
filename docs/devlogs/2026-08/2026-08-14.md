# Development log — 2026-08-14

This snapshot is generated from commits dated 2026-08-14. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `415699c4` 2026-08-14 — feat: split gateway runtimes and add API key integration

## Changed areas

- `internal` — 265 changed-file references
- `contracts` — 39 changed-file references
- `docs` — 13 changed-file references
- `shared` — 5 changed-file references
- `infra` — 5 changed-file references
- `test` — 1 changed-file references
- `go.work` — 1 changed-file references
- `docker-compose.yaml` — 1 changed-file references
- `README.md` — 1 changed-file references
- `Makefile` — 1 changed-file references
- `.github` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
