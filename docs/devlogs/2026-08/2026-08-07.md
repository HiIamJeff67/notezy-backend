# Development log — 2026-08-07

This snapshot is generated from commits dated 2026-08-07. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `0b35e545` 2026-08-07 — refactor: reorganize runtime status probes and Yjs worker architecture

## Changed areas

- `internal` — 75 changed-file references
- `contracts` — 16 changed-file references
- `docs` — 4 changed-file references
- `infra` — 2 changed-file references
- `docker-compose.yaml` — 1 changed-file references
- `docker-compose.prod.yaml` — 1 changed-file references
- `README.md` — 1 changed-file references
- `Makefile` — 1 changed-file references
- `.dockerignore` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
