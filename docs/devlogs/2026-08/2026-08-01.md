# Development log — 2026-08-01

This snapshot is generated from commits dated 2026-08-01. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `e8b29d69` 2026-08-01 — refactor: migrate backend to internal gateway and service boundaries & move the legacy app/shared layout into the internal architecture & establish the phase 2 transport boundaries. - relocate Gateway, Core, shared, platform, contracts, GraphQL, and persistence   code beneath the internal/ layout - introduce versioned API/Core contracts and Gateway-to-Core delegated HTTP   communication - migrate API routes, binders, controllers, middleware/interceptor placement,   route naming, and GraphQL operation contracts - consolidate base exceptions, shared token handling, observability, and config   responsibilities - split DurableJob and Email into independently runnable services with contracts,   transports, Docker wiring, and Core integration - move Core/DurableJob data, repositories, schemas, scopes, and permissions into   their service-local boundaries - update conventions and architecture documentation for the new structure BREAKING CHANGE: legacy app/ and shared/ import paths have been removed. Internal API consumers must use the new contracts/, internal/gateway/, and internal/services/ paths and migrated route contracts.

## Changed areas

- `internal` — 834 changed-file references
- `contracts` — 199 changed-file references
- `app` — 151 changed-file references
- `test` — 15 changed-file references
- `shared` — 8 changed-file references
- `docs` — 8 changed-file references
- `cmd` — 6 changed-file references
- `yjs-worker` — 4 changed-file references
- `infra` — 2 changed-file references
- `main.go` — 1 changed-file references
- `docker-compose.yaml` — 1 changed-file references
- `docker-compose.prod.yaml` — 1 changed-file references
- `README.md` — 1 changed-file references
- `Makefile` — 1 changed-file references
- `.zed` — 1 changed-file references
- `.air.toml` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
