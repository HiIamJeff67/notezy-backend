# Development log — 2026-08-05

This snapshot is generated from commits dated 2026-08-05. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `a0049776` 2026-08-05 — refactor: restructure runtimes, contracts, and event transport

## Changed areas

- `internal` — 120 changed-file references
- `contracts` — 20 changed-file references
- `shared` — 13 changed-file references
- `test` — 10 changed-file references
- `docs` — 8 changed-file references
- `yjs-worker` — 5 changed-file references
- `docker-compose.yaml` — 1 changed-file references
- `docker-compose.prod.yaml` — 1 changed-file references
- `cmd` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
