# Development log — 2026-08-11

This snapshot is generated from commits dated 2026-08-11. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `ab7d5177` 2026-08-11 — feat: add air hot reloading for go runtimes

## Changed areas

- `internal` — 7 changed-file references
- `docs` — 1 changed-file references
- `docker-compose.yaml` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
