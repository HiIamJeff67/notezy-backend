# Development log — 2026-07-14

This snapshot is generated from commits dated 2026-07-14. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `23097cdf` 2026-07-14 — feat: build yjs compaction maintenance and hono worker api & eliminate legacy block mutation services

## Changed areas

- `app` — 27 changed-file references
- `yjs-worker` — 26 changed-file references
- `docs` — 2 changed-file references
- `shared` — 1 changed-file references
- `docker-compose.yaml` — 1 changed-file references
- `docker-compose.prod.yaml` — 1 changed-file references
- `.zed` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
