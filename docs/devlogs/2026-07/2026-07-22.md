# Development log — 2026-07-22

This snapshot is generated from commits dated 2026-07-22. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `a73ecf6c` 2026-07-22 — refactor: elminate useless many to many relationship of routine tag and user

## Changed areas

- `app` — 26 changed-file references
- `shared` — 2 changed-file references
- `docs` — 2 changed-file references
- `docker-compose.yaml` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
