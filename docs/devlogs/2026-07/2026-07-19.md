# Development log — 2026-07-19

This snapshot is generated from commits dated 2026-07-19. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `91d55fa3` 2026-07-19 — feat: add observability for multiplex realtime gateway & add rollout guardrails & add realtime connection admission and distributed user cap and room plan quota

## Changed areas

- `app` — 30 changed-file references
- `infra` — 4 changed-file references
- `shared` — 2 changed-file references
- `go.sum` — 1 changed-file references
- `go.mod` — 1 changed-file references
- `docs` — 1 changed-file references
- `docker-compose.yaml` — 1 changed-file references
- `docker-compose.prod.yaml` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
