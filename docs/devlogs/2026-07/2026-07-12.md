# Development log — 2026-07-12

This snapshot is generated from commits dated 2026-07-12. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `75c6a0a2` 2026-07-12 — feat: refactor yjs-worker & build yjs to block projection layer & refactor to elminate block soft delete feature
- `51de3335` 2026-07-12 — feat: continue on building realtime worker and gateway & continue on building yjs worker
- `fe9cd32c` 2026-07-12 — feat: add zed editor configuration & build yjs worker skeleton with node server & build block pack yjs schemas & build websocket gateway with ticket

## Changed areas

- `app` — 88 changed-file references
- `yjs-worker` — 43 changed-file references
- `shared` — 9 changed-file references
- `.zed` — 8 changed-file references
- `docs` — 6 changed-file references
- `infra` — 2 changed-file references
- `docker-compose.yaml` — 2 changed-file references
- `docker-compose.prod.yaml` — 2 changed-file references
- `go.mod` — 1 changed-file references
- `.gitignore` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
