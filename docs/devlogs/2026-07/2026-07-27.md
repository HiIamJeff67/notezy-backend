# Development log — 2026-07-27

This snapshot is generated from commits dated 2026-07-27. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `f667490e` 2026-07-27 — feat: add block pack and material GraphQL support

## Changed areas

- `app` — 33 changed-file references
- `shared` — 13 changed-file references
- `yjs-worker` — 6 changed-file references
- `docs` — 1 changed-file references
- `docker-compose.yaml` — 1 changed-file references
- `docker-compose.prod.yaml` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
