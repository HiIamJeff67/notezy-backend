# Development log — 2025-08-21

This snapshot is generated from commits dated 2025-08-21. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `408e11ba` 2025-08-21 — feat: modify file structure to prepare the shared submodule & refactor the graphql schemas

## Changed areas

- `shared` — 31 changed-file references
- `app` — 31 changed-file references
- `test` — 1 changed-file references
- `gqlgen.yml` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
