# Development log — 2025-08-24

This snapshot is generated from commits dated 2025-08-24. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `0f6e5afa` 2025-08-24 — refactor: Refactor the dtos package for frontend

## Changed areas

- `app` — 35 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
