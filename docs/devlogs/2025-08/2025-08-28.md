# Development log — 2025-08-28

This snapshot is generated from commits dated 2025-08-28. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `17c10e62` 2025-08-28 — refactor: add LastAnalyzedAt field in shelf table & refactor the search schema in graphql

## Changed areas

- `app` — 15 changed-file references
- `shared` — 11 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
