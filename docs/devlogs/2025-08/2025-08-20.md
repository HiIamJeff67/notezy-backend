# Development log — 2025-08-20

This snapshot is generated from commits dated 2025-08-20. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `7eaf7029` 2025-08-20 — fix: fix some integer usage

## Changed areas

- `app` — 25 changed-file references
- `shared` — 1 changed-file references
- `gqlgen.yml` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
