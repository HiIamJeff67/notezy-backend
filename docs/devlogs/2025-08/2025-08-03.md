# Development log — 2025-08-03

This snapshot is generated from commits dated 2025-08-03. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `b289d56f` 2025-08-03 — refactor: refactor block time calculation & auth code generate constants & exception code in parent domain

## Changed areas

- `app` — 22 changed-file references
- `test` — 2 changed-file references
- `shared` — 1 changed-file references
- `.air.toml` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
