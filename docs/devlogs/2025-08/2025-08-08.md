# Development log — 2025-08-08

This snapshot is generated from commits dated 2025-08-08. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `f0c9cc08` 2025-08-08 — refactor: move validation from input layer to dto layer & only validate the validation tags in services & build some new validation tags

## Changed areas

- `app` — 29 changed-file references
- `shared` — 2 changed-file references
- `test` — 1 changed-file references
- `go.sum` — 1 changed-file references
- `go.mod` — 1 changed-file references
- `.vscode` — 1 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
