# Development log — 2025-08-05

This snapshot is generated from commits dated 2025-08-05. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `1d7642c0` 2025-08-05 — fix: add created at field to user data cache & fix preloads using relation name instead of table name

## Changed areas

- `app` — 15 changed-file references

## Regeneration

Run `make devlog` from the repository root. The pre-commit hook validates the staged result when enabled with `make install-hooks`.
